.DS_Store

# Credentials — this repo's remote is PUBLIC, never commit these.
client_secret*.json
*service*account*.json
.clasprc.json
.env
.env.*
*.pem
*.key

# Personal editor settings — machine-specific taste, not project config.
.vscode/

# Python build artifacts.
__pycache__/
*.pyc

# Local migration safety backup (binary archives + a git bundle).
# Kept on disk because 13 build logs reference it; not source, so not committed.
diane-migration-backup-2026-07-26/
